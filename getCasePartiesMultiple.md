# getCasePartiesMultiple

---

Эта функция не используется начиная с версии Enterprise Case Management 6.2, и будет удалена из будущей версии.
Вместо нее используйте функцию `GetCaseParties()`.

#### Возвращает:

Неизвестно

